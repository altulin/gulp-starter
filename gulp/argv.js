const argv = {
  cache: true,
  debug: true,
  // fix: false,
  // minifyHtml: null,
  minifyCss: null,
  // minifyJs: null,
  // minifySvg: null,
  // уведомления в броузере
  notify: false,
  // открыть страницу при загрузке
  open: false,
  port: 3000,
  // для одностраничного приложения
  spa: false,
  throwErrors: false,
  robots: true,
};

export default argv;
